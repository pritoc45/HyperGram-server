{
  "name": "ayugram-server",
  "version": "1.1.0",
  "description": "AyuGram Pro Server — WebSocket messenger backend (Render-ready)",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "ws": "^8.18.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
